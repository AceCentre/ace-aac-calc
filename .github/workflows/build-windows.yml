name: Build Windows Installer

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  release:
    types: [created]

jobs:
  build:
    runs-on: windows-latest
    env:
      UV_PATH: C:\Users\runneradmin\.local\bin
      VENV_PATH: .venv
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Install and setup uv
      shell: pwsh
      run: |
        iwr https://astral.sh/uv/install.ps1 -useb | iex
        $env:Path = "$env:UV_PATH;$env:Path"
        uv --version  # Verify uv is in PATH
        uv venv  # Create virtual environment
        
    - name: Install dependencies
      shell: pwsh
      run: |
        $env:Path = "$env:UV_PATH;$env:Path"
        .\.venv\Scripts\Activate.ps1
        uv pip install .[build]
        
    - name: Install NSIS
      run: |
        choco install nsis -y
        
    - name: Build executable
      shell: pwsh
      run: |
        $env:Path = "$env:UV_PATH;$env:Path"
        .\.venv\Scripts\Activate.ps1
        uv pip install pyinstaller
        uv run pyinstaller --name scicalc --onefile --noconsole --icon=resources/logo_44I_icon.ico --add-data "src/scicalc;scicalc" src/scicalc/cli.py
        # Verify the executable was created
        if (-not (Test-Path "dist\scicalc.exe")) {
          throw "Failed to create executable"
        }
        
    - name: Build Installer
      shell: pwsh
      run: |
        # Copy icon to dist directory
        Copy-Item "resources\logo_44I_icon.ico" -Destination "dist\"
        # Run NSIS
        & 'C:\Program Files (x86)\NSIS\makensis.exe' scripts/windows-installer.nsi
        # Verify installer was created
        if (-not (Test-Path "ScientificCalculator-Setup.exe")) {
          throw "Failed to create installer"
        }
        
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: windows-installer
        path: ScientificCalculator-Setup.exe
        
    - name: Upload to release
      if: github.event_name == 'release'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      uses: softprops/action-gh-release@v1
      with:
        files: ScientificCalculator-Setup.exe